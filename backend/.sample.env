SECRET_KEY=
# run python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
# in terminal to generate a new secret key
DEBUG_MODE= 
# set to 'True' for development and 'False' for production